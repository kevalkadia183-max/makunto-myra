# Test fixtures

Fictional demo/test data ONLY.

- Never place real YouTube data, real account handles, tokens, or any personal information here.
- The in-app demo personas (Presentation Mode) are generated in code at `artifacts/api-server/src/lib/presentation.ts` and are entirely fictional.
- When adding automated tests, put their seed data in this folder as JSON/SQL fixtures with clearly fake names.
